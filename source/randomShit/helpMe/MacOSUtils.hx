package randomShit.helpMe;

import haxe.Exception;
import sys.io.File;
import sys.FileSystem;
import Sys;
import flixel.util.FlxTimer;
import sys.io.Process;
import Paths;

using StringTools;

/**I'm writing this on a mac! These are utilities for MacOS users.
    @since March 2022 (Emo Engine 0.1.2)*/
class MacOSUtils {
    var userName = Sys.getEnv("USER"); // thank GOD this is the same in linux and macOS.

    
}